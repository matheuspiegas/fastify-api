const userParam = {
    name: 'John',
    age: 30
}

const userDb = {
    name: 'John',
    age: 30
}

const userParamMap  = new Map(Object.entries(userParam));
const userDbMap  = new Map(Object.entries(userDb));
